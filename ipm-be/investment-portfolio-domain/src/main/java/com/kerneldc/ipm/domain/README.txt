The @Description annotation is used to enrch the column attributes with information used be the client and handles by ALPS (Application-Level Profile Semantics)

The available attributes are:

columnDisplayOrder, takes a number
title,
format, takes the string timestamp or currency
sortOrder, takes a number
sortDirection, takes the string asc or desc and defaults to asc if not specified